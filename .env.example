# Example environment variables for speechless project

# Backend Configuration
# URL for the TTS service (used by the backend)
TTS_API_URL=http://tts_service:5000

# Ports exposed on the host machine (optional, defaults are set in docker-compose.yml)
# FRONTEND_HOST_PORT=8080
# BACKEND_HOST_PORT=8000
# TTS_HOST_PORT=5000

# Frontend Configuration (must start with VITE_)
# URL for the backend API (used by the frontend)
VITE_BACKEND_API_URL=http://localhost:8000 