# Python
__pycache__/
*.py[cod]
*.pyd
*.egg-info/
env/
venv/
ENV/
env.bak
venv.bak
.pytest_cache/
.mypy_cache/
.coverage

# Node / Frontend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn
pnpm-lock.yaml
# package-lock.json # (yarn.lockを使う場合)
# yarn.lock # (package-lock.jsonを使う場合)

# Build artifacts
dist/
build/

# OS / Editor specific
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/
.idea/
.cursor/

# Environment variables
.env
.env.*
!.env.example

# Docker specific
docker-compose.override.yml
# BERT model downloaded during Docker build, ignore local copy
tts/bert/

# Logs
*.log

# Other specific ignores
test.wav
ERROR # Check if this specific name is still needed

# Git LFS is configured in .gitattributes for *.safetensors
